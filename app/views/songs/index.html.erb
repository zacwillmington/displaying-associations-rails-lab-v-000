<ul>
    <% @songs.each do |song| %>
    <li><%= link_to song.song_artist_link, song_path(song) %></li>
    <li><%= link_to song.artist.name, song_path(artist_path(song.artist) %></li>
    <% end %>
</ul>